# Default packages for FYERS strategy/bot/backtest code generated by this skill.
# Installed into a project-local venv — see references/setup.md.
fyers-apiv3>=3.1.7
pandas>=2.0
numpy>=1.24
python-dotenv>=1.0
vectorbt>=0.26
